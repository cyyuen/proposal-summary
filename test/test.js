var assert = require('assert');
const file = require('file');


describe('Parser', function() {
  describe('EGSP2-Acc', function() {
    it('should return -1 when the value is not present', function() {
      assert.equal([1,2,3].indexOf(4), -1);
    });
  });
});
